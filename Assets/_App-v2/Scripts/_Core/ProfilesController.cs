using UnityEngine;

public class ProfilesController : MonoBehaviour
{
    //ProfilesController could be create in same wa as PlayerModelBase
    //ProfilesController must change PlayerModelBase.PlPrefsDataKey depending on profile selected/created
}
